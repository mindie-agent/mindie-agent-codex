# P0/P1 follow-up evidence

Experience prompts now request the supplied process and observations, not
lessons or a forced failure/fix story. Missing details are omitted. A real
Luna/max organizer run retained the source NPU measurements in one record in
29.19 seconds. It preceded the final omission wording adjustment; that output
already satisfied the clarification. No automatic model retry occurred.

Actual GitHub feed synchronization with one valid source and one missing
repository now reports `degraded` with both individual results; a real lock
conflict reports `deferred`. Last-good references and bodies survive both.
Successful recovery clears the old error. These are runtime/Git observations,
not a claim of native Stop or publication completion.

On macOS, the production enable/schedule functions generated an isolated real
LaunchAgent. With a 30-second test interval and RunAtLoad disabled, its first
OS dispatch completed in 33.45 seconds, runs=1/exit=0; the installed baseline
remained at main 8ffc72d. The label was removed immediately afterward. This
proves actual timer delivery, while the new feed-result logic was separately
tested against real GitHub operations.

Earlier native installation, rollback, explicit entry and same-task remote-job
continuity evidence remains valid within its recorded scope. Final updated
Hook trust, contribution/consumer integration and Windows real-host acceptance
are separate checks; component tests do not establish them.
