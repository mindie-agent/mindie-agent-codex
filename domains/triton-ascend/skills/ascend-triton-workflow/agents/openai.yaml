interface:
  display_name: "Ascend Triton Evidence Report"
  short_description: "Summarize existing Triton stage evidence"
  default_prompt: "Use $ascend-triton-workflow to consolidate the supplied operator stage evidence into a report and explain its coverage."
policy:
  allow_implicit_invocation: false
