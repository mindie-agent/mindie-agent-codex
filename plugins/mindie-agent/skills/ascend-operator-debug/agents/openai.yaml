interface:
  display_name: "Ascend Operator Debug"
  short_description: "Run isolated operator callables against a reference"
  default_prompt: "Use $ascend-operator-debug to reproduce one operator failure with real callable inputs and compare its outputs against a reference."
