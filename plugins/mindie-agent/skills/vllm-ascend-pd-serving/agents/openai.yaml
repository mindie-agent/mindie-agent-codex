interface:
  display_name: vLLM Ascend PD Serving
  short_description: Orchestrate prefill/decode services and KV-transfer smoke
  default_prompt: Use $vllm-ascend-pd-serving to start and inspect a prefill/decode
    deployment as one coordinator-owned topology.
